name: Archived IP and MAC integration helper

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: No action required
        run: echo "IP and MAC audit details are already integrated in the application."
