name: Archived subnet pool patch helper

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dynamic pool and reservation filtering is already fixed."
